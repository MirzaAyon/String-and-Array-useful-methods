const bigNumbers = [44, 55, 66, 75, 24, 65, 34, 9];
const sortedBigNumbers = bigNumbers.sort(function (a, b) {
    return a - b;
});
console.log(sortedBigNumbers);
//single digit diye korle problem nai double digit diye korle ei problem hbe 